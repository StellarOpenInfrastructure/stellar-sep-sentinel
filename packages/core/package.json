{
  "name": "@stellar-sep-sentinel/core",
  "version": "0.1.0",
  "description": "SEP discovery, conformance checks, test runner and reporters.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@stellar/stellar-sdk": "^17.0.1",
    "smol-toml": "^1.4.0",
    "zod": "^4.6.5"
  },
  "devDependencies": {}
}
